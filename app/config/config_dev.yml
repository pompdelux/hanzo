imports:
    - { resource: config.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }

web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info

assetic:
    use_controller: true

propel:
    dbal:
        default_connection: default
        connections:
            default:
                driver:               %database_driver%
                user:                 %database_user%
                password:             %database_password%
                dsn:                  mysql:host=%database_host%;dbname=%database_name%
                options:              {}
                attributes:           {}
            #     slaves:
            #         slave_1:
            #             user:     %database_slave_1_user%
            #             password: %database_slave_1_password%
            #             dsn:      %database_slave_1_dsn%
            # pdldb2:
            #     driver:               %database_driver%
            #     user:                 %database_user%
            #     password:             %database_password%
            #     dsn:                  mysql:host=%database_host%;dbname=hanzo_db2
            #     options:              {}
            #     attributes:           {}
            # pdldb3:
            #     driver:               %database_driver%
            #     user:                 %database_user%
            #     password:             %database_password%
            #     dsn:                  mysql:host=%database_host%;dbname=hanzo_db3
            #     options:              {}
            #     attributes:           {}
            # pdldbX:
            #     ......
