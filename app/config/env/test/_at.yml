cms:
    frontpage: 538

framework:
    default_locale: de_AT
    session:
        cookie_path: '/de_AT'

propel:
    dbal:
        default_connection: pdldbat1
        connections:
            pdldbat1:
                driver:     %database_driver%
                user:       %test_at_database_user%
                password:   %test_at_database_password%
                dsn:        mysql:host=%test_at_database_host%;dbname=%test_at_database_name%
                options:    {}
                attributes: {}

consignor:
    shipment_server: ~
