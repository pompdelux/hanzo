{% extends layout %}

{% block content %}

<h2>{{ page_title }}</h2>

<ul>
{% for product in products %}
  <li><a href="{{ product.url }}">{{ product.title }}</a></li>
{% endfor %}
</ul>

{% endblock %}
