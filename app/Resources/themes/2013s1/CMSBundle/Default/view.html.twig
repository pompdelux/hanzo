{% extends layout %}
{% block content %}


{% if page_type|default('page') != 'frontpage' %}
  <h1>{{ page.title }}</h1>
{% endif %}
  <article>
    {{ parse(page.content) }}
  </article>

{% endblock %}