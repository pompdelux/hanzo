{{ render(controller('BasketBundle:Default:view', { 'embed': true })) }}
{{ include('CheckoutBundle:Default:order_block.html.twig', {'order': order}) }}

<div class="notice">
  {{ 'late.delivery.notice'|trans({}, 'checkout')|raw }}
</div>
