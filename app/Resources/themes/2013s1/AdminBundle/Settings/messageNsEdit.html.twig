{% extends '::adminbase.html.twig' %}

{% block content %}
<nav class="sub">
  <ul>
    <li>
      <a href="{{ path('admin_settings_messages') }}">
        {{ 'admin.back.button'|trans({}, 'admin') }}
      </a>
    </li>
    <li>
      <a href="{{ path('admin_settings_messages_edit_ns') }}">
        {{ 'admin.messages.add_new.link'|trans({}, 'admin') }}
      </a>
    </li>
  </ul>
</nav>
<h1>{{ 'admin.messages.ns.edit.header.text'|trans({}, 'admin') }}</h1>

{% for message in app.session.flashbag.get('notice') %}
<div class="message notice">
  {{ message|trans({}, 'admin') }}
</div>
{% endfor %}

<p>{{ 'admin.messages.ns.edit.description.text'|trans({}, 'admin') }}</p>

<form action="{{ path('admin_settings_messages_edit_ns', {'id' : message.id}) }}" method="post" {{ form_enctype(form) }} id="messages-ns-edit-form" class="edit-form">
  <div id="messages-ns-edit">
    {{ form_rest(form) }}
    {{ form_row(form._token) }}
  </div>
  <input class="button right" type="submit" value="{{ 'update.messages'|trans({}, 'admin') }}">
  <a class="button left" href="{{ path('admin_settings_messages') }}">{{ 'cancel'|trans({}, 'admin') }}</a>
</form>

{% endblock %}
